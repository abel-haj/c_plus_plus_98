#pragma once

template < typename T >
class Array {

	private:

	public:
		Array <T> ( void ) {
		}

		Array <T> ( unsigned int size ) {
		}

		~Array <T> ( void ) {
		}

		Array <T> & operator = ( Array <T> const & rhs ) {

			return *this;
		}

};
